#include <bits/stdc++.h>
using namespace std;

int R, r, n;
int G[20000][20000];

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0);
    cin >> r >> n;
    R = r * r * r - (r - 1) * (r - 1) * (r - 1);

    int cur = r;
    for (int i = 1; i <= r  
}
